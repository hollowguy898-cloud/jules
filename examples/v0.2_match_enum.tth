// Test match with enum - minimal

enum Color {
    Red,
    Green
}

fn test(c: Color) i32 {
    match (c) {
        Color.Red -> {
            return 1;
        }
        Color.Green -> {
            return 2;
        }
    }
}
