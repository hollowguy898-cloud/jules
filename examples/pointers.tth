// Pointers and references
fn main() i32 {
    var x: i32 = 10;
    var y: i32 = 20;

    // Simple arithmetic with variables
    var sum: i32 = x + y;

    // Mutable counter
    var counter: i32 = 0;
    counter += 5;
    counter += 10;

    return counter + sum;
}
